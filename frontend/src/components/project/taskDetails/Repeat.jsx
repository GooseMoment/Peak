import Detail from "@components/project/common/Detail"

const Repeat = ({ onClose }) => {
    return (
        <Detail title="반복 설정" onClose={onClose}>
            <div>Repeat 창입니다</div>
        </Detail>
    )
}

export default Repeat
